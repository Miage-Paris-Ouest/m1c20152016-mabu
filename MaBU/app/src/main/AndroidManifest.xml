<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="miage.uparis10.fr.mabu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".InfosActivity" />
        <activity android:name=".ProlongationActivity" />
        <activity android:name=".ConsulterPretsActivity" />
        <activity android:name=".RechercherLivreActivity" />
        <activity android:name=".RecevoirAlerteActivity"></activity>
    </application>

</manifest>
